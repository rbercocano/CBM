import { MobilePhoneDirective } from './mobile-phone.directive';

describe('MobilePhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilePhoneDirective();
    expect(directive).toBeTruthy();
  });
});
